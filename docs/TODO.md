# TODO

这份清单记录下个版本优先处理的事项。每次准备发可分发包前，先检查这里，并记得先提升版本号。

## 下个版本优先

- 发布前提升版本号：更新 `pyproject.toml` 的 `project.version`，再重新打包 DMG。
- 针对内存问题做长音频压力测试：ONNX 普通 ASR、ONNX 说话人识别、FunASR 普通 ASR、FunASR 说话人识别分别记录峰值内存。
- 完善 CLI：让 Agent 可以稳定调用单文件/目录转写，并输出机器可读结果路径。cli 的 help 参数能指导 agent 如何使用。
- Windows 迁移验证：在 Windows 上安装依赖、运行 GUI、验证 ffmpeg 打包、测试 PyInstaller 产物。

## 打包与发布

- 为 macOS DMG 加入版本号检查脚本，避免重复用旧版本号覆盖新包。
- 梳理 release 命令，做成一个脚本或 Makefile 目标，减少手工输入环境变量出错。
- 每次打包后记录产物体积：dist app、release app、DMG。

## 产品体验

- 首次启动体验再检查：无模型、模型已存在、模型目录不可写、ffmpeg 缺失等状态都要有清楚提示。
- 说话人识别增加内存风险提示或“省内存模式”：长音频默认更小 diarization 窗口，必要时建议关闭说话人识别。
- 日志窗口继续优化：长任务中显示每个片段耗时、总耗时、预计剩余时间。
- 文件列表增加更明确的状态：未转写、处理中、已完成、失败、已跳过。
- 转写失败时在文件行展示错误入口，方便用户定位日志。
- 考虑给 TXT 输出增加可选标题信息：源文件、创建时间、总耗时、模型引擎。

## 转写质量

- 按 [ASR 模型演进与评测计划](asr-model-evaluation.md) 建立 30 至 60 分钟中文业务基准集和自动评测报告。
- 第一批同机对比：SenseVoice int8、中文 Zipformer int8、中文 Paraformer int8；优先验证更小的中文专项模型能否降低会议/面试 CER。
- 第二批高精度实验：Fun-ASR-Nano int8；Qwen3-ASR 0.6B 和 FireRedASR2S 先观察体积、内存与运行时成熟度。
- 设计模型配置层：模型 ID、引擎类型、语言、版本、文件清单、SHA-256、下载地址和能力标签统一管理。
- 产品层规划 `自动`、`中文优先`、`多语种`、`高精度` 四种策略，非默认模型按需下载到 Application Support。
- 研究 SenseVoice homophone replacer、自定义词典或热词链路，优先改善人名、产品名和英文缩写，不必等待更换大模型。
- 对比 FunASR/PyTorch 与 ONNX 输出质量：准确率、标点、说话人识别、速度、内存占用。
- 评估 VAD 和外层切片配合：确认长音频不会因为 600 秒边界破坏上下文。
- 增加更多测试音频样本：单人、双人访谈、多人会议、背景噪声、英文/粤语/日语/韩语。
- 给缓存增加版本信息：当切片参数、引擎、模型变化时，避免误复用旧缓存。

## 工程整理

- 把发布流程和常用验证命令集中到 README 或单独 release 文档。
- 为 `pipeline` 的进度事件补更多单测，尤其是缓存复用、失败降级、耗时日志。
- 清理临时测试输出目录和历史构建产物，避免误打包无关文件。
- 评估是否需要把 ONNX 和 FunASR 引擎做成更清晰的插件式接口。
- 工程清理修整，改造成可以发布到 github 上的开源项目。用 MIT 协议。

## 已完成

- 0.1.1：DMG 安装界面优化。加入背景图、拖动安装箭头、放大 app / Applications 图标、隐藏 `.DS_Store` 和 `.background`，并使用 `安装{APP_DISPLAY_NAME}` 卷名避开 Finder 缓存。
